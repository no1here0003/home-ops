---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app lidarr
  namespace: downloads
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  maxHistory: 3
  install:
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  dependsOn:
    - name: longhorn
      namespace: storage

  uninstall:
    keepHistory: false

  values:

    defaultPodOptions:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        supplementalGroups:
          - 65539

    controllers:
      lidarr:

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          lidarr:
            image:
              repository: ghcr.io/onedr0p/lidarr-develop
              tag: 2.2.1.4073@sha256:6b7ed356f0a5f2b3f8ea9717ec921b318af7f6e763e661564b641e0560bf2c8c

    service:
      lidarr:
        controller: lidarr
        ports:
          http:
            port: 8686

    ingress:
      lidarr:
        enabled: true
        className: "nginx"
        hosts:
          - host: &host lidarr.${INTERNAL_DOMAIN}
            paths:
              - path: /
                service:
                  identifier: lidarr
                  port: http
        tls:
          - hosts:
              - *host
            secretName: lidarr.tls

    persistence:
      config:
        existingClaim: lidarr

      media:
        type: nfs
        server: "${NAS1_ADDR}"
        path: /volume1/Media/Music
        globalMounts:
          - path: /media

      temp:
        type: nfs
        server: "${NAS1_ADDR}"
        path: /volume1/temp
        globalMounts:
          - path: /temp_dir
